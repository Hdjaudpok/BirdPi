[Unit]
Description=Disable HDMI output to save battery
After=multi-user.target

[Service]
Type=oneshot
ExecStart=/bin/sh -c '/usr/bin/vcgencmd display_power 0 || true; /usr/bin/tvservice -o || true'
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
